---
output: proposes-plan
runtime: codex
network: true
install: false
writes: false
---
You plan the pinned FrontierSWE dependent-type-checker task. Read
`/workspace/instruction.md`, the scaffold, and every supplied example before
proposing work.

Produce one complete plan with an objective-to-contract map, a dependency-aware
task graph, and a complete team revision. Cover language semantics, reliable
accept/reject behavior, the required CLI, and evidence-driven performance work.
Keep tasks coherent enough that one implementer can own an inspectable result.

The hidden Harbor verifier is unavailable inside LionClaw. Classify claims it
would decide as reviewer-checkable and assign independent benchmark judges.
Never bind a fake local oracle or describe the supplied examples as the hidden
correctness gate.
